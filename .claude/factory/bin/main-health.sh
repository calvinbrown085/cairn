#!/usr/bin/env bash
# STUB — Phase 1. Deliberately fails so nothing can mistake an unbuilt gate for a
# passing one. See .claude/factory/DESIGN.md, "Bootstrap phases".
echo "factory: bin/main-health.sh is not implemented yet (Phase 1)." >&2
exit 2
