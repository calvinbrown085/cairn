{
  "$comment": "Runtime knobs for the Stacks factory. See DESIGN.md.",
  "max_parallel": 3,
  "$max_parallel": "Implementers in flight. Ceiling is 3 on this machine; Xcode, not the harness, is the constraint.",
  "max_attempts": 3,
  "$max_attempts": "Implementer attempts before the Opus rescue attempt. After the rescue, the task is parked.",
  "rescue_model": "opus",
  "models": {
    "implementer": "sonnet",
    "reviewer": "opus"
  },
  "dispatchable_types": [
    "implement",
    "chore",
    "debt"
  ],
  "$dispatchable_types": "decide and spike are never dispatched. They queue to the human.",
  "worktree_root": "../stacks-worktrees",
  "derived_data_dir": ".dd",
  "simulator": "iPhone 17 Pro",
  "$simulator": "Single named destination for all sim work, held under state/locks/sim.lock.",
  "invariant_baselines": {
    "system_size_call_sites": null,
    "$system_size_call_sites": "Deliberately null. This number moves with active development (29 at HEAD f93d48f, 57 in the working tree on 2026-08-24). invariants.sh must measure it against the merge-base with main at run time and assert non-increase, never against a number frozen in this file.",
    "allowed_imports": [
      "AppKit",
      "CloudKit",
      "Compression",
      "CoreGraphics",
      "CoreText",
      "CryptoKit",
      "Foundation",
      "ImageIO",
      "MetricKit",
      "NaturalLanguage",
      "OSLog",
      "Observation",
      "PDFKit",
      "PackageDescription",
      "StoreKit",
      "SwiftData",
      "SwiftReadability",
      "SwiftUI",
      "Testing",
      "UIKit",
      "UniformTypeIdentifiers",
      "XCTest"
    ],
    "$allowed_imports": "Apple frameworks only. Anything outside this list fails the gate: the Data Not Collected privacy label depends on there being no third-party SDK, ever."
  },
  "banned_identifiers": [
    "unreadCount",
    "unreadBadge",
    "streak",
    "dailyDigest",
    "readingStreak"
  ],
  "$banned_identifiers": "The guilt-engine ban from ROADMAP.md. A library does not nag.",
  "land_requires_human": false,
  "$land_requires_human": "false: the overseer lands low and medium risk work on its own judgement once the gate is green and /code-review low is clean. The gate has been shown to catch both behavioural and policy regressions, and squash merges keep every landing individually revertible.",
  "$review": "Code review is /code-review low, run by the overseer against the task branch. The overseer separately checks acceptance criteria and the constitution — /code-review does not do that.",
  "human_signoff_risk": [
    "high"
  ],
  "$human_signoff_risk": "Risk levels that still require the human before landing regardless of land_requires_human: anything touching the data model, the reader text stack, the share extension, or project identity. Keep going does not mean ship anything."
}
